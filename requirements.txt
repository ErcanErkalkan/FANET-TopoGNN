# Base scientific stack.
# This file intentionally excludes PyTorch.
# It supports dataset generation, Kinetic-TopoGuard, current-state persistence, shallow/tabular
# baselines, smoke tests, and manuscript table/figure aggregation.
# Configurations containing neural GNN or temporal model names use surrogate
# estimators in this base environment. Install requirements-deep.txt or
# pip install -e ".[deep]" before interpreting GCN/GAT/GraphSAGE/FANET-TopoGNN/
# T-GCN/STGCN/TGN rows as actual PyTorch neural baselines.
numpy>=1.25
pandas>=2.0
scipy>=1.11
matplotlib>=3.7
scikit-learn>=1.3
simpy>=4.1
